# `@nawc/syntax-junit`

JUnit test syntax support for NAWC notebooks.

```sh
pnpm add @nawc/syntax-junit
```

Add `junit()` to the `plugins` array in `nawc.config.ts`.

See [nawc.dev](https://nawc.dev) for syntax documentation.
