# `@nawc/syntax-java`

Java source syntax support for NAWC notebooks.

```sh
pnpm add @nawc/syntax-java
```

Add `java()` to the `plugins` array in `nawc.config.ts`.

See [nawc.dev](https://nawc.dev) for syntax documentation.
