# `@nawc/core`

Core source, runnable, and interactive note elements for NAWC.

```sh
pnpm add @nawc/core
```

Add `core()` to the `plugins` array in `nawc.config.ts`. Most notebooks should enable this plugin.

See [nawc.dev](https://nawc.dev) for plugin documentation.
