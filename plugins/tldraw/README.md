# `@nawc/tldraw`

Interactive tldraw diagrams for NAWC notebooks.

```sh
pnpm add @nawc/tldraw
```

Add `tldraw()` to the `plugins` array in `nawc.config.ts`.

See [nawc.dev](https://nawc.dev) for plugin documentation.
