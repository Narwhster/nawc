# `@nawc/syntax-rust`

Rust source syntax support for NAWC notebooks.

```sh
pnpm add @nawc/syntax-rust
```

Add `rust()` to the `plugins` array in `nawc.config.ts`.

See [nawc.dev](https://nawc.dev) for syntax documentation.
