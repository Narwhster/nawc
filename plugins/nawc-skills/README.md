# `@nawc/nawc-skills`

Agent skills for the NAWC specification and development workflow.

```sh
pnpm add @nawc/nawc-skills
```

Add `nawcSkills()` to the `plugins` array in `nawc.config.ts` to generate the skills while NAWC runs.

See [nawc.dev](https://nawc.dev) for documentation.
