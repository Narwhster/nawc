# `@nawc/syntax-vitest`

Vitest test syntax and execution support for NAWC notebooks.

```sh
pnpm add @nawc/syntax-vitest
```

Add `vitest()` to the `plugins` array in `nawc.config.ts`.

See [nawc.dev](https://nawc.dev) for syntax documentation.
