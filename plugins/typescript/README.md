# `@nawc/syntax-typescript`

TypeScript and TSX source and execution support for NAWC notebooks.

```sh
pnpm add @nawc/syntax-typescript
```

Add `typescript()` to the `plugins` array in `nawc.config.ts`.

See [nawc.dev](https://nawc.dev) for syntax documentation.
