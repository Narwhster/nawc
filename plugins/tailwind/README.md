# `@nawc/tailwind`

Tailwind CSS support for NAWC notebook previews.

```sh
pnpm add @nawc/tailwind
```

Add `tailwind()` to the `plugins` array in `nawc.config.ts`.

See [nawc.dev](https://nawc.dev) for plugin documentation.
