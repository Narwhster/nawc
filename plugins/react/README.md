# `@nawc/react`

React component previews and rendering for NAWC notebooks.

```sh
pnpm add @nawc/react
```

Add `react()` to the `plugins` array in `nawc.config.ts`.

See [nawc.dev](https://nawc.dev) for plugin documentation.
