# `@nawc/provider-opencode`

Connect NAWC conversations to OpenCode.

```sh
pnpm add @nawc/provider-opencode
```

Set `provider: opencode()` in `nawc.config.ts`. OpenCode must be installed and configured.

See [nawc.dev](https://nawc.dev) for provider configuration.
