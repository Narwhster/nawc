# `@nawc/provider-codex`

Connect NAWC conversations to the Codex CLI.

```sh
pnpm add @nawc/provider-codex
```

Set `provider: codex()` in `nawc.config.ts`. The Codex CLI must be installed and authenticated.

See [nawc.dev](https://nawc.dev) for provider configuration.
