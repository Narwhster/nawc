# `@nawc/provider-pi`

Connect NAWC conversations to the Pi coding agent.

```sh
pnpm add @nawc/provider-pi
```

Set `provider: pi()` in `nawc.config.ts`.

See [nawc.dev](https://nawc.dev) for provider configuration.
