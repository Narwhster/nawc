# `@nawc/provider-cursor`

Connect NAWC conversations to Cursor through the Agent Control Protocol.

```sh
pnpm add @nawc/provider-cursor
```

Set `provider: cursor()` in `nawc.config.ts`. Cursor's agent CLI must be installed and authenticated.

See [nawc.dev](https://nawc.dev) for provider configuration.
