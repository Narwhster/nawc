# `@nawc/site`

Build static websites from NAWC notebooks.

```sh
pnpm add @nawc/site
```

Configure a static notebook site, then run `nawc-site build` to generate its deployable output.

See [nawc.dev](https://nawc.dev) for documentation.
