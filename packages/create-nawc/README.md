# `create-nawc`

Create and configure a new NAWC notebook.

```sh
pnpm create nawc
```

The interactive setup configures the notebook directory, package manager, agent provider, editor, theme, project base directory, and plugins.

See [nawc.dev](https://nawc.dev) for the full quick start.
