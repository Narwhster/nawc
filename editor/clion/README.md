# `@nawc/editor-clion`

Open NAWC source references in CLion.

```sh
pnpm add @nawc/editor-clion
```

Set `editor: clion()` in `nawc.config.ts`. The CLion shell launcher must be available on `PATH`.

See [nawc.dev](https://nawc.dev) for editor configuration.
