# `@nawc/editor-cursor`

Open NAWC source references in Cursor.

```sh
pnpm add @nawc/editor-cursor
```

Set `editor: cursor()` in `nawc.config.ts`. The Cursor shell launcher must be available on `PATH`.

See [nawc.dev](https://nawc.dev) for editor configuration.
