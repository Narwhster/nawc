# `@nawc/editor-idea`

Open NAWC source references in IntelliJ IDEA.

```sh
pnpm add @nawc/editor-idea
```

Set `editor: idea()` in `nawc.config.ts`. The IntelliJ IDEA shell launcher must be available on `PATH`.

See [nawc.dev](https://nawc.dev) for editor configuration.
