# `@nawc/editor-vscode`

Open NAWC source references in Visual Studio Code.

```sh
pnpm add @nawc/editor-vscode
```

Set `editor: vscode()` in `nawc.config.ts`. Reveal operations use the `code` shell launcher when required.

See [nawc.dev](https://nawc.dev) for editor configuration.
