# `@nawc/editor-webstorm`

Open NAWC source references in WebStorm.

```sh
pnpm add @nawc/editor-webstorm
```

Set `editor: webstorm()` in `nawc.config.ts`. The WebStorm shell launcher must be available on `PATH`.

See [nawc.dev](https://nawc.dev) for editor configuration.
