# `@nawc/theme-nawc`

Built-in light and dark themes for NAWC notebooks.

The package is installed with `nawc`. Use `nawcLight()` or `nawcDark()` in `nawc.config.ts`, or extend either theme with your own CSS variables.

See [nawc.dev](https://nawc.dev) for theme documentation.
